// Placeholder. `apps/cli/scripts/compile.ts` overwrites this file with a
// `with { type: "file" }` import of the compiled native library right
// before running `bun build --compile`, then restores this placeholder
// afterward. Never edit by hand — the embedded path only exists inside a
// compiled binary, never when running from source.
export default undefined as string | undefined;
